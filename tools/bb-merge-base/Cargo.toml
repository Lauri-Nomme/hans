[package]
name = "bb-merge-base"
version = "0.1.0"
edition = "2021"
description = "Batch merge-base for many (from,to) commit pairs via libgit2, matching `git merge-base`"

[dependencies]
git2 = "0.21"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true