# Phase 0 lab working data (shared homes). Never commit these.
lab/home/
# bb-archiver scrape output (REST dumps + git mirror) — build artifact.
scrape/
# gate3 resumable checkpoint (regenerated each run).
gate3-state.json
*.log
# Bitbucket licenses are credentials; never commit them.
lab/license*.txt
# Agent memory store.
.mnemonics/__pycache__/
__pycache__/
*.pyc
tools/bb-obj-tar/target/
tools/bb-merge-base/target/
# Cross-compile cargo config carries machine-specific osxcross paths; the
# recipe lives in tools/BUILD.md instead.
tools/bb-obj-tar/.cargo/
tools/bb-merge-base/.cargo/
